package tutoriallist;

public class TutorialList {
    /*
    Declare the instance variables of the class.
    We want to have just an integer array called "data"
    and an integer called "size".
     */

    /*
    Create two constructor methods.
    One should take no arguments and initializes data as an empty array and size to 0.
    The second should take one parameter being an array and data should be initialized to the argument array
    and size should be the length of the argument array.
     */

    /*
    Create the methods 'size()', 'get()', 'set()', two 'add()' methods, 'swap()', 'toArray()' and 'toString()'
     */
}
